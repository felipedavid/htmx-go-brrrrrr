{{define "base"}}
<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>{{template "title" .}}</title>
</head>
<body>
	{{with .Flash}}
		<div class="flash">{{.}}</div>
	{{end}}
	{{template "main" .}} 
</body>
</html>
{{end}}
